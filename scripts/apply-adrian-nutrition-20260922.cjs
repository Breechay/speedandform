'use strict';
// One-time, scoped source update. It does not deploy, change an assignment or touch native code.
const fs=require('node:fs'), path=require('node:path'), vm=require('node:vm'), assert=require('node:assert/strict');
const root=path.resolve(__dirname,'..');
const file=path.join(root,'labs/adrian-runner-mass/index.html');
const data=JSON.parse(fs.readFileSync(path.join(root,'docs/studies/ADRIAN-NUTRITION-INTAKE-20260922.json'),'utf8'));
let html=fs.readFileSync(file,'utf8');
function bounds(text){const start=text.indexOf('const STUDY = ');assert(start>=0,'STUDY marker missing');const from=text.indexOf('{',start);let depth=0,quoted=false,escaped=false;for(let i=from;i<text.length;i++){const c=text[i];if(quoted){if(escaped)escaped=false;else if(c==='\\')escaped=true;else if(c==='"')quoted=false;continue;}if(c==='"'){quoted=true;continue;}if(c==='{')depth++;if(c==='}'&&--depth===0)return [from,i+1];}throw Error('STUDY object did not close');}
const [a,b]=bounds(html);const S=JSON.parse(html.slice(a,b));assert.equal(S.id,'FRM-001');
const before=JSON.parse(JSON.stringify(S));
const nutritionPath='/plans/adrian-nutrition-phase-01/';
const esc=v=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
if(S.version!=='2026.09.22.1'){
 assert.equal(S.version,'2026.09.21.1','Study changed concurrently; review before applying');
 S.version='2026.09.22.1';
 S.athlete.summary="Adrian runs a 2:53 marathon at nineteen. By his own account he has never sustained a gym routine before this block. The question is whether he can add muscle and strength while preserving running quality. Food, sleep, running and strength are all being recorded; a later change cannot automatically be attributed to just one of them.";
 const mass=S.baseline.find(x=>x.k==='Body mass');assert(mass&&mass.v===null,'Do not overwrite an established baseline');mass.note='156 lb athlete-reported; standardized weekly mean pending';
 S.baseline.push({k:'Reported body mass',v:'156 lb',note:'Athlete report, not a standardized baseline mean',s:'report'});
 S.nutrition={...S.nutrition,status:'Nutrition 01 authored',why:'A reliable breakfast and prepared afternoon meal now support the work lunches and family dinners already available.',note:'<a href="'+nutritionPath+'">Fuel Your Work: Adrian’s nutrition page</a>. Published instructions are not adherence or an outcome. The actual start date, full intake and standardized mass trend remain to be filed.',bias:['Breakfast with whole milk, chocolate whey, bread, peanut butter and a navel orange.','Spaghetti with meat sauce prepared for the afternoon.','Work lunch and family dinner stay in place.','Confirmed likes and dislikes remain on the nutrition page.','Earlier phone cutoff and bedtime; review the actual sleep trend.'],notYet:['No measured nutrition-driven muscle gain or performance change.','No exact calorie surplus or final body-weight target.','Actual start date, meal portions, tolerance and adherence remain open.','No automatic FORM or Forge synchronization is claimed.']};
 S.adherence=[...S.adherence,'Sep 22 intake: breakfast is inconsistent; work is about 8–5, with provided lunch, a fridge and a microwave. Family dinners are available.','Reported bedtime/wake window is midnight–5:30 a.m.; that is 5.5 hours available, not measured sleep.','Adrian explicitly asks for concrete instructions. Greek yogurt, cottage cheese and Brussels sprouts are excluded.'];
 S.timeline.push({id:'nutrition-intake-filed',when:'Sep 22',kind:'Intake + nutrition instructions',type:'note',title:'Fuel Your Work',body:'Reported 156 lb, food preferences, work storage and recovery context are filed. Nutrition 01 specifies breakfast and pasta prep. Actual start, adherence and outcomes are not yet recorded.'});
 S.archive=[...(S.archive||[]),before.currentRead];
 S.currentRead={date:'2026-09-22',n:'03',body:'A 156 lb working value is now athlete-reported. The standardized mass trend, circumferences and comparison photos remain pending. No hypertrophy result is claimed.',running:'The 44-mile Week 02 remains planned work. Historical race screenshots, reported bests and watch estimates are now separated in the dated intake record; none establishes current fitness by itself.',next:'Start the nutrition routine, record the actual start date, and review adherence, appetite, digestion, sleep and run response. Four-day upper soreness prompts review of the performed dose before progression. Existing run and strength prescriptions are unchanged.'};
 S.queue=S.queue.map(q=>q[1]==='Typical training-day intake and meal timing'?[q[0],'Full intake and portions: 2 normal work lunches and 2 family dinners']:q[1]==='Sleep duration and recovery notes'?[q[0],'Actual sleep trend and recovery after nutrition routine begins']:q);
 S.queue.push(['Athlete','Nutrition 01 start date and first 7-day adherence/tolerance review']);
 S.intake20260922=data;
 for(const k of ['season','races','running','mileageBuild','media','photos','protocol','targets','strength','measurements'])assert.deepEqual(S[k],before[k],'Protected study field changed: '+k);
 html=html.slice(0,a)+JSON.stringify(S,null,1)+html.slice(b);
}
const marker='<!-- ADRIAN INTAKE 20260922 -->';
if(!html.includes(marker)){
 const rows=data.historical_results.map(r=>`<tr><td>${esc(r.date_as_displayed)}</td><td><a href="${esc(r.url)}" target="_blank" rel="noopener">${esc(r.event)}</a><p>${esc(r.detail)}</p></td><td><strong>${esc(r.result)}</strong><p>${esc(r.source_status)}</p></td></tr>`).join('');
 const section=`${marker}\n<section id="intake-20260922" class="sec"><div class="shell"><p class="lbl">22 September 2026 / Intake and source update</p><h2>Food, recovery and the race record.</h2><p class="intake-lead">The first nutrition routine is authored. The response has not happened yet.</p><p><a class="intake-link" href="${nutritionPath}">Fuel Your Work · Open Adrian’s nutrition page ↗</a></p><div class="intake-cols"><div><h3>What is now known</h3><p>156 lb is an athlete-reported working value, not a standardized baseline. Work is about 8 a.m.–5 p.m., with provided lunch, a fridge and microwave. Family dinner remains available.</p><p>The nutrition page keeps a named breakfast and afternoon pasta meal, whole milk, selected fruit, food preferences and a preparation schedule. Greek yogurt, cottage cheese and Brussels sprouts are excluded.</p></div><div><h3>Recovery still needs attention</h3><p>The reported midnight–5:30 a.m. window allows 5.5 hours in bed. Four days of upper soreness is athlete-reported; severity and actual performed loads still need review. Neither proves a protein deficiency.</p><p>Start date, full intake, adherence, standardized measurements and training response remain open. Food, sleep and strength can change together; this single-athlete record cannot isolate their effects.</p></div></div><h3>Historical results: keep the sources separate</h3><p>Transcribed from the supplied Athlinks screenshots. Direct result links are included where provided. Search-list entries are not upgraded to independently verified race-detail records. Dates below are dates displayed by Athlinks, not a claim that event-wrapper dates equal the race day.</p><div class="intake-table" tabindex="0" role="region" aria-label="Historical race-result sources"><table><thead><tr><th>Date displayed</th><th>Event and source note</th><th>Result</th></tr></thead><tbody>${rows}</tbody></table></div><details><summary>Unresolved results and device estimates</summary><p><strong>10K:</strong> 35:15 is the athlete-reported best. The supplied 2024 Rockin’ Rib listing says 35:21. The difference is unresolved; do not silently change either or assume chip versus gun time.</p><p><strong>Identity check:</strong> the same-name Courthouse 53:02 entry lists M25 and Hialeah. It is excluded until identity is resolved. The Eugene entry’s location metadata also needs reconciliation.</p><p><strong>Watch context:</strong> reported COROS VO₂max 65 and an 18:03 5K prediction are device estimates. Reported threshold 6:05 is interpreted as per mile in the conversation, with a direct current screen still requested. VDOT 54.5 is a different metric. Historical sub-17 cross-country races and the 4:57 mile remain athlete reports until result sources are filed.</p><p><strong>Measurement:</strong> 3–4 consistent morning weights per week and a weekly mean. Standardized photos every two weeks, preferably before training and without a pump. The newly reported weight is not evidence of weight gained.</p></details><p class="intake-small">Scope: nutrition companion and source filing only. No run or strength assignment, completed record or native app has been changed. Nutrition starts on the athlete’s actual start date, not automatically on publication.</p></div></section>\n`;
 assert(html.includes('</main>'),'main closing tag missing');html=html.replace('</main>',section+'</main>');
 const css=`<style id="adrian-intake-20260922">#intake-20260922 h2{font-size:clamp(28px,4vw,48px);line-height:1.12;max-width:22ch;margin:12px 0 18px}#intake-20260922 h3{font-size:21px;line-height:1.25;margin:25px 0 12px}#intake-20260922 p{font-size:17px;line-height:1.65;max-width:76ch;margin:0 0 16px}#intake-20260922 .lbl,#intake-20260922 .intake-small{font-size:14px;line-height:1.6}#intake-20260922 .intake-link{display:inline-block;padding:12px 18px;border:1px solid currentColor;border-radius:28px;text-decoration:none;margin:8px 0 20px}#intake-20260922 .intake-cols{display:grid;grid-template-columns:1fr 1fr;gap:32px}#intake-20260922 .intake-table{overflow-x:auto;margin:20px 0}#intake-20260922 table{border-collapse:collapse;width:100%;min-width:640px;font-size:16px}#intake-20260922 th,#intake-20260922 td{text-align:left;vertical-align:top;padding:15px 12px;border-bottom:1px solid var(--rule)}#intake-20260922 td p{font-size:14px;margin-top:8px}#intake-20260922 th:first-child{width:145px}#intake-20260922 td:last-child{width:210px;font-variant-numeric:tabular-nums}#intake-20260922 summary{cursor:pointer;min-height:54px;padding:14px 0;font-size:17px;font-weight:700}#intake-20260922 details{margin:18px 0} @media(max-width:700px){#intake-20260922 .intake-cols{grid-template-columns:1fr;gap:0}}</style>\n`;
 html=html.replace('</head>',css+'</head>');
 html=html.replace('<a href="#timeline">Journal</a>','<a href="#timeline">Journal</a><a href="'+nutritionPath+'">Nutrition</a>');
}
html=html.replace(/"dateModified"\s*:\s*"2026-09-18"/,'"dateModified":"2026-09-22"');
for(const m of html.matchAll(/<script\b([^>]*)>([\s\S]*?)<\/script>/gi)){if(/application\/ld\+json/.test(m[1]))JSON.parse(m[2]);else if(!/src=/.test(m[1]))new vm.Script(m[2]);}
const [c,d]=bounds(html);const after=JSON.parse(html.slice(c,d));assert.equal(after.version,'2026.09.22.1');assert.equal(after.baseline.find(x=>x.k==='Body mass').v,null);
assert.equal((html.match(/id="intake-20260922"/g)||[]).length,1);assert(html.includes(nutritionPath));
const n=fs.readFileSync(path.join(root,'plans/adrian-nutrition-phase-01/index.html'),'utf8');assert(n.includes('<h1>Fuel Your Work</h1>'));assert(n.includes('Prepare the meals you tend to miss.'));assert(n.includes('21 g of protein'));assert(n.includes('Brussels sprouts.'));assert(!/\bhe likes\b|\bhe enjoys\b/i.test(n));assert(!/Greek yogurt|cottage cheese|Brussels sprouts/.test(n.slice(n.indexOf('<section id="your-day">'),n.indexOf('<aside>'))));
fs.writeFileSync(file,html);
const roadmap=path.join(root,'docs/roadmap/FORM-ROADMAP.md');let r=fs.readFileSync(roadmap,'utf8');const heading='## September 22 - Adrian: Fuel Your Work and intake record';if(!r.includes(heading)){const note=`\n${heading}\n\nAdrian-only nutrition companion at /plans/adrian-nutrition-phase-01/. Confirmed preferences, whole milk, selected fruit, exact whey and prep instructions are linked from the existing study. Input record: [source and decisions](../studies/ADRIAN-NUTRITION-INTAKE-20260922.json). Athlete-reported values, Athlinks detail screenshots, listing-only records and device estimates remain separate. Same-name identity conflicts are excluded. No strength/run assignment or native app change. Actual nutrition start and outcomes are pending.\n\n- [x] Source-authored page and dated study update.\n- [x] Protected training/history fields checked unchanged by scoped update.\n- [ ] Production URL verification; source alone is not a live claim.\n- [ ] Physical phone review and first athlete check-in.\n\n`;r=r.replace('\n','\n'+note);fs.writeFileSync(roadmap,r);}
console.log('Adrian update passed: nutrition authored, evidence separated, training and reference assets preserved.');
