# FORM public plan static visual prototype v1

Purpose: reproduce the approved image mockup before any production/data wiring.

Approved deviation from the reference: the upper-left image tile is removed. No other intentional redesign.

Run locally:

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000

Reference: `reference.png`
Rendered capture: `capture-1536x1024.png`

This prototype intentionally uses static representative schedule data from the approved visual reference. Production wiring is a later phase.
