forge/index.html
media/*.orig.mp4
mockupc/media/*.orig.mp4

# Local Netlify folder
.netlify

# Apple signing key + generated client secret. Never committed: publish = "." means
# anything tracked here is downloadable from speedandform.com.
keys/

# Render harness: production dump wired into the real modules so a surface can
# be checked without a session. Rebuilt from scripts/dump_plan.sh output.
harness/

# Design-review export. Generated by scripts/build_design_review.py from the
# real renderer plus the plan dump — 12 MB of copies, rebuildable in one
# command. The sources that are not copies live in scripts/design-review/.
form-labs-design-review/
form-labs-design-review.zip
form-labs-design-review-v2.zip
form-labs-design-review-v1/
