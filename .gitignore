forge/index.html
.DS_Store
media/*.orig.mp4
mockupc/media/*.orig.mp4

# Local Netlify folder
.netlify

# Apple signing key + generated client secret. Never committed: publish = "." means
# anything tracked here is downloadable from speedandform.com.
keys/

# Render harness: production dump wired into the real modules so a surface can
# be checked without a session. Rebuilt from scripts/dump_plan.sh output.
harness/

# Design-review export. Generated by scripts/build_design_review.py from the
# real renderer plus the plan dump — 12 MB of copies, rebuildable in one
# command. The sources that are not copies live in scripts/design-review/.
form-labs-design-review/
form-labs-design-review.zip
design-review/public-plan/plan.json

# Consolidated 2026-09-09 physical reorg pass: dated design-review/plan-html
# snapshots that used to sit loose at the repo root now live under _generated/
# (see _generated/README.md). form-labs-design-review/ itself stays covered
# above — the generator still writes there.
_generated/

# Whole-repo backup zips (self-referential — a zip of this repo sitting inside
# it). Added 2026-09-09 physical reorg pass per the original audit's own
# recommendation. speedandform-source.zip lived at root; now archived under
# 90_ARCHIVE/Backups/ (see that folder's README) but still worth ignoring by
# pattern wherever a future one lands.
speedandform-source.zip
90_ARCHIVE/
