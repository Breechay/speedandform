---
description: speedandform web repo — doctrine pointers and denylist for agents.
alwaysApply: true
---

# speedandform — Agent context

**Human tone (workspace default):** Site = understanding (philosophical, timeless); still human, not deck-speak. `FORM-iOS/docs/BRICE_OS/HUMAN_TONE_DEFAULT.md`. FORM web spec governs tone split with app.

## Read first

1. `docs/CURSOR_CONTEXT.md`  
2. `docs/DO_NOT_BUILD.md`  
3. When **FORM-iOS** is in the same workspace: `FORM-iOS/docs/form_field_doctrine.md` and `FORM-iOS/docs/form_web_spec_v2.md` for The Field and `/form` behavior  

## Defaults

- **Diagnose before redesign** (especially loading/auth/env).  
- **Preserve** production static site and `coach.html` unless the task explicitly migrates them.  
- **No** social / engagement mechanics — see `docs/DO_NOT_BUILD.md`.
