# V6.1 drop-in update

Unzip into the repository root. This overwrites `index.html`, adds `assets/home/site/sf-mark.png`, and adds the V6.1 handoff note. Existing FORM/FORGE assets and routes are untouched.
