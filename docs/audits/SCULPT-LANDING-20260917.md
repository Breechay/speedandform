# Breechay Sculpt / Forge: Pass 5B

Owner: Brice. Single register: `docs/roadmap/SITE-ECOSYSTEM-PASSES-20260916.md`.

## State

IMPLEMENTED in the isolated `work/forge-landing-pass5b` branch. Not yet a production receipt. Final tested commit, browser results, merged commit, deployment and live-byte checks will be recorded after they actually complete.

## Explicit owner interruption

The owner requested José rather than Runner inside the FORM website illustration and Adrian in an equivalent Sculpt illustration. `/form/` changes only `RUNNER` to `JOSÉ` in the existing illustrative identity. A regression compares the entire page with main and allows that exact single replacement only. The existing FORM design-preview and example-session labels remain. `/forge-sculpt/` uses `ADRIAN.` in its app illustration, labeled sample training. No real athlete profile, program, session, assignment, receipt, native source, or account was read or changed to make these illustrations.

## Bounded product release

One product landing page, `/forge-sculpt/`, is rebuilt in its own warm-black, bone and bronze world, with stronger Jost body text and locally hosted existing fonts. FORM's native-sans dark field remains distinct. The reading-page cream stylesheet, Labs, homepage, coaching funnel, purchase flow, native apps, training portal and private interfaces remain unchanged. 175 other HTML sources are held byte-for-byte against base `7eb2106a4ab2ef29fa26b98bbc062dfed900c192`; the second HTML change is the exact owner-requested José substitution.

The page leads with a plain outcome: “Look like you train on purpose.” It explains how the emphasis moves, makes all twelve phase names inspectable in three campaigns, shows an actual App Store image, presents the gym/time/experience commitment, and routes directly to the current Breechay app.

The old launch-notification form is removed, not rewired or replaced by an unverified email collector. `#notify` remains a compatibility anchor at the current start section. Other meaningful section bookmarks remain. No subscriber data is queried, erased, imported or sent. Existing legal/support URLs and authenticated training routes remain intact.

The existing sample-set concept is retained as an explicit, ephemeral website demonstration: adjust weight and repetitions, complete the sample, inspect exactly those values, reset. It does not save, sync, upload, or file a real session. Invalid and blank values receive errors with focus recovery. No-JavaScript readers get the example and working download links instead of inert controls.

The old dedicated sharing image says “four-phase,” contradicting the twelve-phase program. The landing page now uses `assets/forge/og-sculpt-20260917.jpg`, a 1200 × 630 capture of authored HTML using the existing approved anatomy illustration. It contains no price, countdown, review or athlete result. The previous image file is retained for compatibility; unrelated page cards are not replaced.

## Product and design evidence

Primary availability and public purchase description: Apple's live lookup for app ID `6790569283`, captured by read-only Actions `35206426281` and `35207088651` on September 17. The current lookup reports **Breechay 3.4**, released September 15 at 01:25:38 UTC, minimum iOS 17. The description states twelve phases, week one free to train, and U.S. **$199.99/year, automatically renewing**, managed through Apple. The page discloses the annual renewal and cancellation terms and refers local pricing to Apple. A free first training week is not relabeled a seven-day subscription trial. No StoreKit, app listing, subscription product or price was edited.

The web-search snapshot still reported version 2.1, and the older `forge/ship` branch is an August 11 / 2.1 submission. Neither is used to claim the current downloadable version. The direct Apple lookup is the release evidence. The actual 600 × 1299 App Store image copied without pixel editing has SHA-256 `8e5c5392188de4a8498fe05ca69889a6e5bfaf560daa89b94225128d5afd913f` and remains separate from the Adrian illustration.

Visual and behavioral sources read:
- Website `docs/FORGE_DESIGN_NORTHSTAR.md`, `docs/FORM_TYPOGRAPHY_STANDARD.md`, `forge-sculpt/FORGE_WEBSITE_APP_CONTINUITY_BRIEF_20260730.md`, and athlete-language rules.
- Native `docs/FORGE_START_HERE.md`, `docs/FORGE_CANON.md` Book I, and `docs/FORGE_LEDGER.md` on the older `forge/ship` branch, interpreted as historical where a newer owner rule supersedes them.
- Current native `codex/forge-adrian-access-reconciliation` at `66f7ddaa1b840519dbca1c78e70e44b196826512`: `AGENTS.md`, `docs/FORGE_ADRIAN_CURRENT_STATE.md`, and the Forge aesthetic gate. The September rule explicitly permits self-directed placement in accessible authored content; the July “no phase picker” claim must not be repeated. This website pass does not imply that an unverified working-branch change has shipped to every installed phone.

The three-campaign phase names are retained from the authored continuity brief. Exact 49-week/267-session/Hold-implementation claims, a universal six-day claim for every phase, Sunday conversion language, fabricated allocation percentages, a workout-marketplace promise, and unverified completion/access gates are deliberately absent. The opening week is identified as six sessions; later weeks depend on their phase. Requirements are for experienced lifters with a full commercial gym, not a beginner's course or a home-dumbbell plan.

## Review and acceptance

Conversion surface: prepared space, readable hierarchy and authentic product evidence. Bronze marks labels, identity and a sample's primary action; no decorative glow, stacked dividers, animation theater or persistent motion is introduced. The anatomy yields to reading at very narrow available widths, including 200% zoom.

Initial in-memory Chromium review covered six widths and open content at 200% zoom. It caught a genuine narrow-column overflow: navigation, allocation rows, sample controls and the decorative numeral. Container-based reflow now stacks them rather than hiding overflow or shrinking all text. A contrast guard also caught the paper identity dot below the normal-text target; it now uses the same darker bronze as other paper labels. This is design/source review, not a served-page or physical-device claim.

The container's administrator policy blocks localhost browser navigation. That read-only limitation was respected; full served-page and production browser acceptance runs in the already-used GitHub Actions environment. Twelve earlier source suites, all existing page browser suites, a new Sculpt source suite and actual live verification are release gates, not claims about tests already completed.

Physical iPhone/iPad, Safari, screen-reader output, App Store purchase dialogs and native sharing sheets remain open unless later evidence is recorded. No app-store purchase, coaching inquiry, email, social post or signup is sent during tests. No indexing, ranking, conversion or numerical design-quality score is claimed.

## Maintenance

Edit `forge-sculpt/index.html`, `css/sculpt-landing.css`, `js/sculpt-landing.js`. The demonstration and public page are not a second training engine. Keep private data and program access outside this surface. Preserve the documented provenance and labels of its images.

`SCULPT-LANDING-MANIFEST-20260917.json` pins the page, eight referenced public assets, and the exact José-only change. The older FORM manifest advances only that HTML/body checksum. The discovery manifest advances only the two reviewed page checksums. Regression tests accept the explicit new snapshot, not arbitrary later changes. No fonts are included in downloadable review artifacts.

Pass 6 (contact, Field Notes, optional updates and final journey review) remains separate. This release does not change the native app or delivery state of Adrian's coaching pilot.
