# FORM — reachable surfaces · 19 Aug 2026

Open in Chrome. Phone is the real test.

Live: https://speedandform.com/ · twin: https://speedandform.com/mockupc

**Production `/` is closed.** The four-plate homepage is live. Do not reopen 03 on `/` for taste.

`/mockupc` is ahead of this zip: race line under the disc (`snap37`). Four segments, no labels. The light travels. Dragon crop on the twin is being read as a form entering, not a shard. Placard still queued.

Cache: `/` `rd38`. `/mockupc` `snap37`. The reachable zip packed at close-out is stale on mockupc until repacked.

## `/` — production

```
01  THE WORK         colour film
02  THE PRACTICE     grey film
03  THE INSTRUMENT   bone paper. Dial is the sun. Dragon is weather.
04  THE START        first question on film B
BEGIN                persists. Ink on cream. Still opens at question one.
```

01 + first thesis + Begin is still complete without reaching 03.

The review is a receipt: *Here is what you told me.* What they said, nothing invented. No Hope Note.

Send gates on FormSubmit’s **success** flag, not HTTP 200. On failure, a prefilled mailto. Video is not attached to that POST.

Payment is **Zelle, arranged in the reply**, after what you would do first. Not Stripe. Not a cart. `docs/claude-handoff/AFTER-SEND.md`.

Brice must click the FormSubmit confirmation email once or nothing forwards.

## 03 — the instrument (closed)

The disc is the sun. The week is at rest. The dragon is atmosphere, not a third object.

- Paper `#F2EEE6`. Disc `#F8F4EC`, `60%`. Centre `35svh + 14px`.
- Radial mask. Week labels static blur. MON/SUN gone on the phone. SAT/TUE further in the fade. The ring does not light.
- Session: 7s hold. Outgoing 0.5s, incoming 0.95s from 0.45s. Never both legible.
- Tail: `/media/dragon-tail.webp`, own `<img>` layer, cropped through the bottom-right. No cream pocket. Mid-right of the phone stays empty. Warmed on 02, not preloaded against the films.
- `FORM · iOS` opens the App Store.

Read `docs/claude-handoff/INSTRUMENT-PLATE.md` and `docs/claude-handoff/SURFACE-AND-WEATHER.md`.

`/mockupc` is the same four plates, `noindex`, plus the race line. Film B there is `/mockupc/media/practice.mp4`. Production uses `/media/practice.mp4`. Do not copy the line onto `/` until asked.

## Later (not this zip)

The Field stays complete while bare. Weather on the app — dragon in unused field, maybe a slight shift with the day, clouds if they stay quiet — waits. Paywall waits. Athletes first.

Art-book / poster references are confidence, not a layout to copy onto Today.

## Files

| Path | What |
|---|---|
| `index.html` `home.css` `home.js` | Production `/` |
| `mockupc/` | Four-plate twin |
| `media/` | Films, posters, `dragon-tail.webp` (not `.orig.mp4`) |
| `notes.html` `notes.css` | NOTE 001 |
| `og/` | Share cards |
| `docs/CURSOR_CONTEXT.md` | Orientation |
| `docs/DO_NOT_BUILD.md` | Denylist |
| `docs/claude-handoff/` | Specs. Start with `SURFACE-AND-WEATHER.md`, `INSTRUMENT-PLATE.md`, `AFTER-SEND.md`. |

## Do not

- Add a cart or rebuild a checkout
- Put Hope on the review
- Invent a mark the app does not use
- Bake the tail into a CSS background, or cover the disc
- Redesign 03 because you have a new idea for the dragon
- Rewrite `coach.html`
- Start the iOS Field illustration or paywall from this zip
