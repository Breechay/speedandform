# Soundtrack — parked

Tried on `/mockupc`. Canned. Not on production `/`. Do not rebuild.

## What was tried

One unmarked Play on 02, next to `@form.practice`. One `<audio>` outside the plates so 01 → 02 → 03 → Begin did not restart it. Off in the bar after it had started. Intake ducked volume instead of pausing. No bar, no artist, no scrub, no playlist. Silent on arrival.

The bed was a Mixkit stand-in (Deep House by Arulo). Temperature, not Massh / iLanga.

## Why it is parked

The control did not belong on 02. Finding Play was already the idea failing: if it has to be explained, it is not an instrument. A bottom bar was refused earlier for the same reason — Begin already owns that edge.

The films stay silent.

## Do not

- Put a player on `/` or `/mockupc`
- Autoplay
- A bottom transport
- Bring this back unless Brice asks
