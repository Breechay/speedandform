# Soundtrack — `/mockupc` only

Not on production `/`. Do not copy this to `index.html` until the bed inhabits the films.

## What it is

One room tone for the three plates. Play lives on 02, next to `@form.practice`. No bar, no artist, no scrub, no playlist.

Silent on arrival. One `<audio id="bed">` outside the slide track, so 01 → 02 → 03 → Begin does not restart it. After it has started, **Off** appears bar-right (and stays available during intake). Asking/reading ducks volume to 0.35; it does not pause.

## Track (stand-in)

Temperature, not the record. Not Massh. Not iLanga.

| | |
|---|---|
| File | `mockupc/media/bed.mp3` (96 kbps, ~1.8 MB, loops) |
| Title | Deep House |
| Artist | Arulo |
| Source | [Mixkit, Deep House](https://mixkit.co/free-stock-music/deep-house/) — asset `196` |
| License | [Mixkit Stock Music Free License](https://mixkit.co/license/#musicFree) — commercial use, attribution not required. Do not redistribute the file as a standalone download. |

Swap the file without touching the controls if this cheapens the films. Organic / Afro house, ~110–122 BPM, no EDM build, no stock-Africa cliché.

## Do not

- Autoplay
- A bottom transport
- Ship this to `/` from a Claude zip
- Call `load()` on the audio from the film watchdog
