# Track E — weather. Hand this to Codex

**Repo:** `FORM-iOS`  
**Surface:** Today  
**Source of truth:** live `https://speedandform.com/` plate 03, cache `rd48`  
**This package:** 19 Aug 2026. The dragon treatment from the live site, not from memory.

Track C landed. Today is the instrument. This is the extra Track C did not do.

Open `TRACK-E.md` and do **E-weather only**. Do not start E1–E5 (chapter close, Week Print, marks, haptics). Do not reopen Track C. Do not reopen Track D.

## Why this exists

Track C was told not to port the dragon. That was right then. Today is now the instrument. Weather goes around it — same animal, same treatment as the site.

The law is in `SURFACE-AND-WEATHER.md`. The crop, the mask, the drift are in `extracts/`. The art is in `media/`. Look at `ref/` and at live `/` on a phone.

## Files

| File | Use |
|---|---|
| `TRACK-E.md` | Build this. |
| `SURFACE-AND-WEATHER.md` | The law. Read before drawing. |
| `TONE.md` | Site vs app. The dragon is silent. |
| `ROADMAP.md` | Ledger. Mark E-weather `[x]` when done. Leave E1–E5 alone. |
| `ref/01-plate-dragon.png` | Live phone. Dragon, no cloud. This is the look. |
| `ref/02-plate-placard.png` | Same room, placard open. Weather still does not cover the work. |
| `extracts/` | Live CSS and markup for the tail. No cloud. |
| `media/dragon-tail.webp` | The art. Also `dragon-tail.png` for the asset catalog. |
| `FORM-TRACK-E.zip` | This folder, zipped. Give this to Codex. |

## What not to port

- The cloud. Parked. Dirt smudge, then a cut-off kumo. Do not restore.
- Site copy. *Every session is built for something* stays on the site.
- A weather widget, a forecast, a second sun.
- Chapter close, Week Print, haptics. Those are later Track E. Not this zip.

## Gate

Today is already the instrument. Do not rebuild C1–C8. Put weather in unused field and stop.
